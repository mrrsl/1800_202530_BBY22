{
  "name": "planoria",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "vite .",
    "build": "vite build",
    "deploy": "vite build && firebase deploy"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  },
  "dependencies": {
    "express": "^5.1.0",
    "firebase": "^12.6.0"
  }
}
